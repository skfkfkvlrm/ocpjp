package daily_study.day_19.part_01;

/*
**Q3. Which is the correct way to convert a stream of strings into uppercase
* and collect into a list?**
A. `stream.map(toUpperCase()).toList()`
B. `stream.map(String::toUpperCase).collect(toList())`
C. `stream.upper().collect()`
D. `stream.toUpperCase().toList()`
*/

public class Q3 {
}
